# Local backups created by cleanup scripts
backup_*/

# Python virtual env (local)
.venv/

# Python cache
__pycache__/
*.pyc

# Environment secrets
.env
.env.*
.tmp_cf_vars.txt

# Review folder
da-revisionare/

# Photoshop sources (large, not for production)
*.psb
*.psd


# Internal notes / source assets (keep local or in Obsidian, not in production repo)
notes/
berny video/
# Mojibake repair backups (temporary)
_mojibake_backup_*/

